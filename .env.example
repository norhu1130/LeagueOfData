# Optional AI assistance through OpenRouter. Keep the real key in .env, never in source control.
LOD_OPENROUTER_API_KEY=
LOD_OPENROUTER_MODEL=openai/gpt-5.6-luna
LOD_OPENROUTER_TIMEOUT_SECONDS=45

# Riot Developer API. Used only by `lod-data collect` and `lod-data crawl`.
RIOT_API_KEY=
