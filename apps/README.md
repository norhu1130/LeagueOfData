# Applications

Runnable user-facing applications live here. Shared domain behavior belongs in `packages/`, while server-side execution belongs in `services/`.

- [`web`](./web/) — the React analysis workspace

Run all local application dependencies with `pnpm dev` from the repository root.
