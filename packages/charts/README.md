# `@lol/charts`

Pure result-shape-to-visualization decisions plus lightweight React rendering adapters.

`pickChart` selects KPI, bar, line, table, distribution, comparison, or map presentations from result metadata. Invalid alternatives include an actionable explanation. Rate axes default to the full 0–100% range.

The package does not fetch data or own analysis state.
