# Services

Long-running backend processes live here. Services consume contracts from the TypeScript reference artifacts and Python data package rather than redefining domain semantics.

- [`api`](./api/) — FastAPI lifecycle management, plan compilation, and DuckDB execution
